 function changeText(paragraphId) {
    // Define new text based on the button clicked
    var newText = "Hello, KK PATIDAR";
    // Set the new text to the corresponding paragraph
    document.getElementById(paragraphId).innerHTML = newText;
}